# example_0

Minimal Vite + React todo list using `mut-state`.

## Run

```bash
cd examples/example_0
bun install
bun run dev
```

## What It Shows

- `create(...)` for writable state
- `useStore(...)` for component subscriptions
- `derive(...)` for computed remaining count
